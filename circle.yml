machine:
  xcode:
    version: 7.3

dependencies:
  override:
    #- brew update
    #- brew upgrade carthage || true
    - bundle
    - carthage update --platform ios

test:
  override:
    - rake
    #- rake test:integration:setup_and_run
